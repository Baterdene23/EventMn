"use client"

export type ToasterProps = {
  richColors?: boolean
}

/**
 * Local placeholder Toaster.
 *
 * You can later replace this with `import { Toaster } from "sonner"` after installing `sonner`.
 */
export function Toaster(_: ToasterProps) {
  return null
}
